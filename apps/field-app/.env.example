# Browser/local default:
VITE_API_URL=http://localhost:3000
VITE_API_TOKEN=dev-token

# Native packaged builds must override both values with reachable, non-localhost
# settings before `pnpm --filter @v2e/mobile build`.
