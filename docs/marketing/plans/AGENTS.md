# Marketing site — surface plans

Campaigns, homepage iterations, section sequencing, and **marketing-only** milestones (no product API contracts).

## Critical UI implementation rule

- Enact UI is the shared design-system base across surfaces.
- For reusable **product execution** UI patterns, implement common components in `apps/field-app/src/components` to preserve the V2E theme-overlay layer on top of Enact UI.

| Plan / note | Purpose |
|-------------|---------|
| _(add markdown files here)_ | |

## Reminder

- Marketing-only page components stay local to marketing implementation, but any cross-surface execution UI pattern should be routed through `apps/field-app/src/components`.

Parent: [`../AGENTS.md`](../AGENTS.md).
